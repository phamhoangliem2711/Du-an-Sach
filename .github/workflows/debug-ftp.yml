name: Debug FTP Login
on: [workflow_dispatch]
jobs:
  debug:
    runs-on: ubuntu-latest
    steps:
      - name: Test FTP Connection
        run: |
          echo "Testing FTP credentials..."
          echo "Username: if0_40677219"
          echo "Note: Testing connection (password hidden)..."
          
          # Test FTP connection (dry run)
          curl -v --disable-epsv --ftp-skip-pasv-ip ftp://ftpupload.net/ 2>&1 | head -20
